/speckit.plan
Create an implementation plan for feature branch `002-effort-estimator`
using specs/002-effort-estimator/spec.md.

Technical constraints:

- Frontend-only React + TypeScript single-page application.
- Do NOT mention or assume Vite, CRA, Next.js, or any specific bundler by name.
- Say instead: "Use the team's preferred React build tool" whenever build tooling is described.
- Keep all business logic for MD and role allocations in pure functions in a separate module.
- Input and Output should be two views in the same SPA, controlled by React state.
- Include sections for: component hierarchy, state model, calculation helpers, and basic testing strategy
  (Jest + React Testing Library as an example, but not required).

Write the plan into specs/002-effort-estimator/plan.md.
